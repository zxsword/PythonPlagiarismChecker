# Python代码查重与自动批改系统 —— 敏感配置模板
# 使用方法：将此文件复制并重命名为 .env，然后填入真实值。
# .env 已加入 .gitignore，不会被提交到版本控制，请放心填写。
#
# 三种填入方式（优先级由高到低）：
#   1. 系统环境变量：export PLAGIARISM_API_KEY=你的key   （服务器/CI 推荐）
#   2. 本文件 .env：直接在下方填写                       （日常使用推荐）
#   3. 软件界面的【⚙️ AI设置】：图形界面填写后自动写入此文件

# API Key（支持 Gemini、DeepSeek、Kimi 等所有 OpenAI 兼容格式的服务）
PLAGIARISM_API_KEY=

# 本地 HTTP 代理地址（国内直连 Gemini 通常需要；使用 DeepSeek 等国内服务可留空）
# 示例：http://127.0.0.1:7890
PLAGIARISM_API_PROXY=
